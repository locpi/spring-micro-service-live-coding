spring:
  datasource:
    url: jdbc:mysql://localhost/ducks
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ducks
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: '*'


